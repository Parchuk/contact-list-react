import { HoukBus } from 'houk';
import { Contently } from '.';
declare type Runner = (this: ContentlyPlugin, ...options: any) => void;
interface PluginConstructor {
    name: string;
    runner: Runner;
    instance: Contently;
    options: any[];
}
export declare class ContentlyPlugin extends HoukBus {
    name: string;
    protected instance: Contently;
    private _runner;
    constructor({ name, runner, instance, options }: PluginConstructor);
}
export {};
